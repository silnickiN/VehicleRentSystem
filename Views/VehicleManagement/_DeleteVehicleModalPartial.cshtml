@model VehicleModel

<div class="modal fade" id="DeleteVehicle-@Model.Id" aria-labelledby="DeleteVehicle" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Delete vehicle</h4>
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form asp-action="DeleteVehicle" asp-route-id="@Model.Id"  method="post">
                    <div class="form-group">
                        <label asp-for="Id">ID: @Model.Id</label>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProductionYear">@Model.ProductionYear</label>
                        <label asp-for="Manufacturer">@Model.Manufacturer</label>
                        <label asp-for="Model">@Model.Model</label>
                    </div>

                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-dark" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-outline-danger">Delete</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

</div>